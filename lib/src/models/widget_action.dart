enum WidgetAction {
  showAlert,
  navigate,
  updateState,
  showSnackbar,
  toggleTheme,
  openUrl,
  logMessage,
  refreshData,
  submitForm,
  cancelOperation,
  pickImage,
  shareContent,
  playSound,
  vibrateDevice,
  copyToClipboard,
  pasteFromClipboard,
  toggleVisibility,
  toggleEnabled,
  toggleSelected,
  startAnimation,
  stopAnimation,
  incrementCounter,
  decrementCounter,
  resetCounter,
  openDrawer,
  closeDrawer,
  toggleSwitch,
  selectDate,
  selectTime,
  captureScreenshot,
  sendEmail,
  makePhoneCall, 
  printMessage, 
  logEvent, 
  openURL, 
  scanQRCode, 
  showSnackBar, 
  launchMap, 
  openWebView, 
  validateInput, 
  showDatePicker, 
  showTimePicker, 
  toggleLanguage, 
  downloadFile, 
  uploadFile, 
  startTimer, 
  stopTimer, 
  resetTimer, 
  openSettings, 
  logout, deleteItem, 
  archiveItem, 
  favoriteItem, 
  unfavoriteItem, 
  rateApp, 
  contactSupport, 
  bookmarkPage, 
  unbookmarkPage, 
  shareLocation, 
  toggleSound, 
  toggleNotifications,
  showModalBottomSheet, 
  closeModal, 
}
